# Run on main only to allow fast commits during PR.
# On other branches, skip pre-commit (PR CI is the gate).
branch=$(git branch --show-current)
if [ "$branch" != "main" ]; then
  exit 0
fi

make pre-commit
