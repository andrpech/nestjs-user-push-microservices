NODE_ENV=production
PORT=3002

RABBITMQ_URL=amqp://guest:guest@rabbitmq:5672

# Cron schedules — slower cadence for production-like load
USERS_CRON_EXPR=*/30 * * * * *
NOTIFIER_CRON_EXPR=*/30 * * * * *
